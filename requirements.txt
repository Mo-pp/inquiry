# 从项目根目录的 .env 加载秘密配置。
python-dotenv>=1.0,<2.0

# 当前 FastAPI 示例入口已有此依赖；后续统一服务入口继续使用。
fastapi>=0.115,<1.0

# 运行 FastAPI 的本地 ASGI 服务。
uvicorn[standard]>=0.30,<1.0

# Apps Script HTTP 客户端
httpx>=0.27,<1.0

# MySQL 事务和 leads 入库。
mysql-connector-python>=9.0,<10.0
