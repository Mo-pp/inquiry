数据从谷歌表单
->
gateway_client
            网关负责：读取统一配置，向 Apps Script 发送请求：
            - get_tail()：获取当前末行和下一行号。
            - read_rows(start_row)：从指定行开始，按配置批量读取。
            - 校验返回结果，明确报告超时、认证失败和数据格式错误。

lead_mapper.py
    负责：将表单的24 列转换成 leads 表字段
       和清理空格和空值。
        将 true/false 转成布尔值。
        将带时区的时间转换成 MySQL 可保存的时间。
        标准化 whatsapp_number。
        保存原始 headers 和 values 到 raw_data。
        第一列 id 为空时跳过该行。
        表头数量不足或顺序不符合预期时直接报错。

models.py
    定义一个 LeadRecord 数据对象，统一表示转换后的线索，避免后续数据库代码直接操作不明格式的列表。

lead_repository.py
        作用：
        - 接收 LeadRecord。
        - 将 24 列数据对应到 leads 表字段。
        - 批量执行 INSERT。（使用数据库事务：整批成功才提交，失败则回滚。
                            返回统计结果：- 新增多少条；- 跳过多少条；)

sync_service.py   执行增量同步流程服务

runtime_state.py      保存内存状态<br/>接收开关、下一行号、同步中标记<br/>上次时间、错误和统计


----------------------------------------------------------------------------------------
拼装模板
first_contact_template.py  拼装模板


Python 服务                      wa-bridge                  WhatsApp
    │                                │                          │
    ├─ HTTP：添加联系人（号码）──────→   │─ 执行添加 ─────────────→│
    │←──────── 返回结果 ─────────────  ┤                          │
    │                                │                          │
    ├─ HTTP：发送消息（对象、正文）──→    │─ 执行发送 ─────────────→│
    │←──────── 返回发送结果 ──────────  ┤                          │



    wa-bridge 接口
  -----------------------------------------------------
    流程确定为：
1. 扫描 leads 中未联系的线索。
2. 检查号码是否注册 WhatsApp。
3. 已注册 → 拼装并发送模板消息。
4. 发送成功后，在一个数据库事务里：
   - 在 customers 表创建客户；同号码已有客户就复用。
   - 在 messages 表保存这条发送记录。
   - 回写 leads.customer_id，设置 customer_added = TRUE。
5. 后续扫描跳过这条已联系线索。
检查已注册不算已联系，发送成功才算；不需要等客户回复。