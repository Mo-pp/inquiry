# 复制本文件为 .env 后填写真实值。不要把真实秘密提交到源码管理。

# Apps Script Web 应用的 /exec 地址。
GOOGLE_APPS_SCRIPT_URL=https://script.google.com/macros/s/DEPLOYMENT_ID/exec

# 必须与 Apps Script 脚本属性 ACCESS_TOKEN 完全相同，建议至少 32 位。
GOOGLE_APPS_SCRIPT_TOKEN=replace-with-a-long-random-secret

# MySQL 密码；开启同步前填写。
MYSQL_PASSWORD=
